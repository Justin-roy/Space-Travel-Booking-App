import 'package:flutter/material.dart';

class PlanetVerticalDivider extends StatelessWidget {
  const PlanetVerticalDivider({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 40,
      width: 1.8,
      color: Colors.white,
    );
  }
}
